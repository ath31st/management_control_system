server:
  port: 28880

topic:
  delivery:
    name: t.delivery
  order:
    name: t.order
  catalogue:
    name: t.catalogue

shop:
  name: shop1

catalogue:
  url: http://localhost:28889/api/products

spring:
  kafka:
    #    bootstrap-servers: "kafka:9092"
    bootstrap-servers: "localhost:9093"
    consumer:
      auto-offset-reset: earliest
      group-id: "default"