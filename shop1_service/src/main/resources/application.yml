server:
  port: 28880

topic:
  delivery:
    name: t.delivery
  order:
    name: t.order

shop:
  name: shop1

spring:
  kafka:
    #    bootstrap-servers: "kafka:9092"
    bootstrap-servers: "localhost:9093"
    consumer:
      auto-offset-reset: earliest
      group-id: "default"